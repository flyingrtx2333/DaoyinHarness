# 2026-09-06 Windows 全仓验证

本记录覆盖用户要求一并提交的 Harness 内核、本地工作台、账号隔离、云端界面与文档改动。验证环境为 Windows PowerShell、Node.js 22.23.2。

| 检查 | 结果 | 证据范围 |
| --- | --- | --- |
| `npm run typecheck` | 通过 | 全工作区类型检查 |
| `npm run lint` | 通过 | 全仓代码检查 |
| `npm test` | 54 个文件、293 项通过 | 单元、策略、真实临时 SQLite、回放与模拟身份/模型 |
| `npm run build` | 通过 | 全工作区构建 |
| `node scripts/verify-workspace-ui.mjs` | 7 项通过，无页面错误 | Windows Edge、真实本地服务器/文件工具、模拟模型/目录选择器 |
| `node scripts/verify-account-access.mjs` | 9 组通过，无页面错误 | Windows Edge，1280/1920/390/320 像素，本地构建与模拟平台 API |

原有崩溃恢复测试使用 `resource_fixture` 伪造工作区范围，被新内核的身份/资源校验正确拒绝；测试现使用真实临时目录的范围标识，并验证续接请求被接收、任务最终完成、恢复提示及既有工具证据确实进入模型。跨资源历史仍由独立拒绝用例覆盖，没有放宽生产策略。

账号访问浏览器检查覆盖同账号重连保留草稿、切换账号清除旧会话、退出显示道引登录入口、官网访客入口，以及已登录官网用户无密码重输/无手动凭据进入 Harness。工作区检查覆盖选择、切换与运行中切换保护。

本记录描述本地验证，不替代真实私人账号登录。随后已完成云端内核升级及真实公开模型/检索验收，实际部署版本、数据库备份与操作见 [独立上线记录](../deployment/RELEASE-20260906-ACCOUNT-ACCESS.md)。
