# 工作台业务插件

插件入口位于左侧“插件”和输入框左下角“＋”。插件页支持按名称、说明和功能搜索；选择已接入插件后返回会话，保留当前草稿。待接入项展示业务能力，不提供虚假的安装或启用按钮。

## 所有插件的默认权限规则

登录后，所有已接入业务插件自动获得该账号在当前空间内已有的完整业务权限，覆盖读取、写入、生成和管理。以后接入的插件采用相同规则，无需逐插件授权、勾选范围、人工签发或粘贴凭证，不设置额外的 Agent 权限子集。已有账号连接自动复用；外部服务缺少有效连接时只完成该服务必要的登录。完整权限的定义、业务规则与实现边界见 [ADR-0012](adr/0012-first-party-account-access.md)。

## 2026-09-11 盘点

| 插件 | 已有业务能力 | 此云端工作台状态 | 源码依据 |
| --- | --- | --- | --- |
| 官网知识 | 公开知识检索、产品问答、来源引用 | 已启用 | `packages/server-cloud/src/workbench-profile.ts` |
| 赛事只读 | 赛事、设备、素材、地图、人物时间线、任务状态 | 已启用 | `packages/server-cloud/src/saishi-profile.ts` |
| 快速视频 | 文生视频、参考图片/视频生成、历史与进度 | 已启用 | [Story 快速视频](STORY-QUICK.md) |
| 网站与应用 | 创建/修改网站和轻应用、预览 | 待接入 | 主平台 `backend-builder/README.md` |
| 文旅影像 | 场景人像、AI 图片编辑 | 待接入 | 主平台 `backend/services/ai_image_edit.py`、`backend/tests/test_ai_image_edit_youji.py` |

该表说明代码中的业务接口基础；未接入项未在本轮进行真实业务运行验收，不能据此声称已成为可用 Harness 插件。

## 选择与执行

`packages/ui/src/cloud/plugins.ts` 是界面盘点清单，不签发权限。登录后的工作台固定使用 `daoyin-workbench` Profile，一次提供官网知识、赛事和快速视频工具；Agent 按用户意图调用。服务端仍逐次验证账号、业务订阅、资源归属和费用归属。

会话绑定统一 Profile，不绑定单个插件。已接入项始终显示“已启用”，无需手动选择、打开或切换；待接入项只展示状态。未识别的 Profile 会停止提交，不通过前端状态扩大业务权限。

刷新页面沿用 HttpOnly 账号会话和统一会话空间，不通过 `?app=` 拆分历史。后续业务插件遵循 [同账号完整权限规则](adr/0012-first-party-account-access.md)；未接入工具仍需完成实际实现后才能列为已启用。

## 短剧接线进度

现有短剧 MCP 提供能力发现、受控业务调用、媒体上传、视频工作流、任务查询/等待和审计。接入时从登录身份自动解析个人或企业空间、完整业务权限、项目资源和付款归属，再把任务与作品结果返回同一 AgentEngine。保留写操作幂等性与原有业务交易规则，不增加插件授权或重复确认已明确发出的操作。工作台插件目录本身不完成这些接线。

2026-09-11：快速模式已并入统一 Profile，同账号桥接、上传和视频任务卡片已上线。完整短剧项目/分镜编辑未在此次开放。
