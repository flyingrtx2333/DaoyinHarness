# A 版侧栏验收

已实现用户选择的 A：新建入口优先、导航随后、历史标题与搜索相邻、历史条目有图标、底部身份及官网同排。所有元素为真实 DOM，复用现有 Logo 和 SVG 图标；没有新增业务 API 或部署。

Windows / Node.js 22.23.2 的 `typecheck`、`lint`、`build` 通过，`git diff --check` 通过。完整单元测试为 259 通过、2 失败：`packages/server/src/app.test.ts` 的深层分叉临时目录清理 ENOTEMPTY，以及中断恢复提示等待失败。未修改该服务端文件，不将这些失败写为侧栏通过或已修复。

本地 Edge + 云端生产方式打包预览 + 模拟业务 API：8 组交互检查通过、25 张截图、无意外控制台错误或失败请求。包含发送/取消、引用、重连/刷新回放、插件目录与弹层、过期授权；增加 A 版顺序、共享 240px 宽度、键盘顺序、官网链接、空搜索、44 条历史独立滚动、长标题及手机选择后关闭抽屉检查。见 [浏览器报告](run-2026-09-06T02-36-50-998Z/browser-report.json)。

## 图像对照与明确偏差

选中概念和 [实际页面](run-2026-09-06T02-36-50-998Z/sidebar-a-reference.png) 均为 1672×941。见 [数值报告](comparison.json)、[差异图](diff.png) 和 [叠加图](overlay.png)。整体 MAE 0.03106、相似度 0.96894、容差内像素 93.41%。但侧栏区域 MAE 0.03933，关键边界也不满足原图 3px 差异门槛，因此不声称原始概念像素复刻通过。

五项主要偏差按影响记录如下；这些偏差遵循用户已明确要求的全站参数一致性，不通过扩大字号或留白去消除：

| 区域坐标（概念图） | 类型 | 偏差与处理 |
| --- | --- | --- |
| x=0–364，y=0–941 | 布局 | 概念侧栏约 364px，实际按共用规范为 240px；功能顺序保持 A。 |
| x=364–1672，y=0–941 | 布局 | 概念扩大了主区卡片；实际保留先前已统一的顶栏、目录最大宽度和卡片尺寸。 |
| x=26–338，y=106–288 | 字号/控件 | 概念按钮及导航约 50px 高；实际桌面保持 36px 控件、13px 导航。 |
| x=26–338，y=345–650 | 字号/间距 | 历史区域按 12px 标题、13px 条目、共享间距排布，较概念紧凑；列表真实滚动。 |
| x=26–338，y=827–926 | 布局/字号 | 底部区域按共享字号合并，实际 y≈869 起；官网仍在身份旁。 |

当前验收结论：A 的结构和响应式交互已实现，全站参数约束通过；原始生成图的像素一致性不是通过项。无真实模型或线上验收声明。
