# C 版插件目录验收

已实现紧凑卡片：保留图标、名称、短用途和一个状态/操作，删除能力标签、引导句及重复说明。桌面卡片112px起，正文14px、标题16px、padding16px；窄屏单列，按钮移到文字下方，文字增大时自然增高。目录样式集中在cloud.css，移除cloud-modern.css重复覆盖。A版侧栏和会话页通过相邻流程回归。

实现期间共享工作区加入“赛事只读”及授权状态，因此最终清单为五项。保留该并行功能；未授权项显示“授权”而非“使用”，本轮不改变授权逻辑、不执行授权。待接入三项仍无按钮。概念为四项不构成删除当前功能的理由。

## Windows 验证

Node.js22.23.2：typecheck、lint、build、git diff --check通过；最后两处文案/焦点色调整后，云端重新打包预览和相关文件lint通过。全仓库测试260通过、1失败，失败为未修改的server/src/app.test.ts中断恢复提示检查。本轮插件清单及授权呈现相关测试已通过。

[浏览器报告](run-2026-09-06T02-54-22-956Z/browser-report.json)记录9组检查、25张常规截图；另存1张同尺寸内容截图和4张200%文字压力截图。真实Edge、本地生产方式打包、模拟API，无真实模型或生产操作。无意外控制台错误/请求失败。

覆盖桌面2073/1672/1280、390/320手机；插件及侧栏、隐藏能力关键词搜索、无结果、键盘Tab/Enter使用、执行中禁用、待接入无动作、发送取消、重连回放、过期授权；320/390/640/1280下200%文字及长标题检查均无卡片内部重叠或横向溢出。新加入的赛事授权服务本身不在本轮UI验收范围。

对比度工具测得说明文字#667183对白4.94:1、按钮#3864e8对#edf2ff为4.51:1。目录焦点使用共享强调色并保留2px轮廓。未执行人工读屏、真实触控设备、网络慢速压力或Core Web Vitals测量，不将这些项目视为通过。

## 概念对照

[选中原图](selected-reference.png)与[实际内容区域](run-2026-09-06T02-54-22-956Z/plugin-c-reference.png)均为1576×998；[对比报告](comparison.json)、[差异图](diff.png)、[叠加图](overlay.png)已保存。全图MAE0.01292，相似度0.98708，容差内97.16%；搜索和第一行局部差异超默认阈值，边界也不满足3px，所以不声称原图像素复刻通过。

五项主要偏差及处理：

| 原图区域 | 类型 | 处理 |
| --- | --- | --- |
| x129–1445，y242–553 | 数据/布局 | 当前五项比原图四项多一张授权卡片，保留真实清单而非伪造数据。 |
| x129–1445，y242–379 | 布局 | 采用共享112px卡片高度、1120px容器与16px间距；不沿用放大的概念尺寸。 |
| x0–1576，y0–90 | 布局/字号 | 顶栏保持全站52px而非概念约90px。 |
| x1100–1445，y132–188 | 布局 | 搜索保持280×36px并与目录右边缘对齐。 |
| x158–540，y271–347 | 资产/字号 | 既有SVG图标36px、标题16px、正文14px；不采用概念放大图标和字体。 |

以上调整遵循用户持续要求的全站一致性。结果为C版结构和文案预算落地，非整图按像素放大。尚未部署。
