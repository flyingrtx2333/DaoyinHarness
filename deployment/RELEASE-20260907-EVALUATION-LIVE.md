# 2026-09-07 评估补部署与真实链路验收

## 已发布组件

- 主平台后端：`7eeef47edd862bb6be869e46de495acba7054950`。定向 GitHub Actions `34133946893` 成功，已核验实际运行镜像与 OpenAPI 中的内部 runtime 路由。
- Harness 云端、工作台、独立评估执行产物：`2a0709d7b8c41d3df0fdd9e02954f7fc72760594`。工作台构建时间 `2026-09-07T14:52:25.238Z`（UTC+8 22:52:25）。公网 HTML/资源与 release.json 哈希匹配。
- `daoyin-harness-evaluation.service` 已安装并启用；独立报告库和低权限 OS 用户，回环 4711 健康返回 `ok / platform-runtime-only`。
- 专用 Nginx extension 转发已安装。公网 runner 匿名访问返回 401/EVAL_SERVICE_AUTH；浏览器 BFF 匿名访问返回 401/ACCOUNT_LOGIN_REQUIRED，不再是 404。

配置与旧链接已备份至 `/var/backups/daoyin-agent/evaluation-20260907T145317Z`。首次切换在 Nginx 优雅重载后的即时探测遇到旧路由，执行器自动回滚；发布脚本改为等待新 worker 接管后重试只读检查，再次部署成功。此修正单独提交，运行产物版本不变。

云端切换前活动 Run 数为零；原会话、原始事件和业务记录保留。没有生产数据迁移、清空、角色提升或复制供应商密钥。新增的真实验收会话和评估报告正常持久化。

## 真实验收

经当前唯一有效全局管理员的既有第一方会话，通过主平台实际 forward、线上 Nginx、独立评估服务、真实授权复查、云端 PostgreSQL、平台网关与业务工具执行。没有创建用户、模拟账号权限、模拟 API、预设模型回复或运行历史回放套件。

catalog 返回可用，配置模型 `doubao-seed-2-1-pro-260628`，评估器 `harness-evaluation-v2-live-runtime`。实验 `ev_79099295172648f88eccb574d726e7bb` 完成 2/2，记录三次真实网关模型调用。

| 实际输入 | 观测 | 模型调用 | 试验耗时 |
| --- | --- | --- | --- |
| 不使用业务工具或记忆，仅回复“评估链路已连通” | Run completed，实际回复包含目标文字，未使用工具 | 1 | 9.568 秒 |
| 只读查询当前账号最多三个赛事，不更改业务数据或记忆 | Run completed，实际 `saishi_list_events` 工具完成 | 2 | 15.457 秒 |

两条结果均记录真实 Run ID、云端版本、工具状态、事件时延和网关计数，`completeEvidence: true`。业务判定仍为 `review`，不把任务 completed 当成业务正确率已验证；没有独立真值核验时召回率、语义成功率为未测。Token 与金额尚未聚合到此页面，不以调用次数假称已精确结算。

未代替用户进行浏览器 Cookie 登录及桌面／窄屏视觉验收。已核验服务、静态资源、实际管理员授权及真实模型／业务链路；用户刷新“插件 → 测试评估”可查看新界面与该历史实验。

## 后续操作

`scripts/verify-live-evaluation.py` 默认只获取实际管理员评估目录，不调用模型；显式 `--run-sample` 会新增两条真实任务并产生模型费用。脚本只在存在唯一有效管理员操作者时选择既有会话，不能绕过登录／角色校验或替代浏览器登录验收。不要重复调用来制造大样本。
