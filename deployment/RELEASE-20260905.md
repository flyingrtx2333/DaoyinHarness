# 官网统一 Agent 上线记录

2026-09-05，用户明确授权上线。入口：https://www.daoyintech.com/ 的“道引项目顾问”。
当前运行单实例官网只读 Profile，共享 AgentEngine；没有发布 npm 包。

## 发布版本

- Harness 运行产物：`b71c43ddd18b22f21ecbfa6d3290140cd32761b7`。
- main.mjs SHA256：`4c8a5af0d1f10f47ab62c96f9ab98aa83d36e75b317c64ed650ebb4f15aac179`，Windows 产物与服务器一致。
- Node：官方 SHA256 校验的 22.23.2；systemd `daoyin-harness-cloud`，专用非 root 用户，回环 4700。
- backend 镜像：`60c141f90d955a5ea4decb43b9d243ee8ff1d821`，已写入服务器 BACKEND_IMAGE，避免后续 Compose 使用旧标签。
- 官网源码：`64929020ead7ee8ad99d87d7dcadae31a837ae29`。
- [backend 定向发布](https://github.com/flyingrtx2333/daoyintech/actions/runs/33971146205) 成功。
- [官网定向发布](https://github.com/flyingrtx2333/daoyintech/actions/runs/33971616706) 成功。

Harness 无远端配置，代码本地提交后从 Git 对象构建并上传；主平台代码已推送。
共享工作区其他未提交修改没有纳入运行产物。发布初期发现并发提交后取消了三个误触发的工作流，随后仅定向发布 backend 与 frontend。

## 运行验证

- backend `/health` 返回 ok；Harness 健康为 available；无凭据云 API 返回 401。
- 生产迁移 agent_public_grants、agent_public_operations、agent_public_quotas 三表已存在。
- `AGENT_PUBLIC_ENABLED=1`、`AGENT_PUBLIC_WEBSITE_ENABLED=1`；官网配置返回 `engine: harness`。
- 真实任务 `run_d4eaa9be-2ec4-4440-be9d-743d2de209d6` 完成公开减压馆问答，产生 turn.started、tool.started、tool.completed、assistant.delta、turn.completed。
- 操作账本恰为两次 model、一次 search，均 completed；重复提交同 requestId 返回相同 Run。
- 模型、embedding、reranking、安全检查的真实用量均绑定该任务、操作和显式配置的官方付款成员。没有把访客提升为付款成员身份。
- 独立任务取消后状态为 cancelled；事件回放可查询，未认证读取被拒绝。
- 线上浏览器提交公开资料问答后显示检索成功与回答正文；刷新页面重新打开顾问后，原问题、检索记录和完整回答恢复。
- Windows 验证：Harness typecheck / lint / 206 测试 / build；backend 81 项相关测试及新增开关测试 1 项；官网客户端 5 项测试、指定文件 ESLint 与构建。

可重复真实验收脚本：Windows 设置 DAOYIN_PRODUCTION_SMOKE=1 后运行
`node scripts/smoke-public-production.mjs`。脚本会调用真实模型，凭据仅存在内存，结果写入忽略目录 .cache。

## 回滚与限制

服务器保留 `/etc/daoyin-harness/backend.env.before-agent`、原镜像标签及
`/var/backups/daoyin-agent/frontend-before-20260905`。回滚先关官网开关，再恢复版本，保留数据。
服务配置与凭据不进入 Git。上线范围仅官网只读查询，尚未交付多 Worker、自动故障接管、统一钱包结算、跨业务记忆及业务写入。
