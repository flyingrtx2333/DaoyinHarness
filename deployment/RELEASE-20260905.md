# 官网统一 Agent 上线记录

2026-09-05，用户明确授权上线。入口：https://www.daoyintech.com/ 的“道引项目顾问”。
当前运行单实例官网只读 Profile，共享 AgentEngine；没有发布 npm 包。

## 发布版本

- Harness 运行产物：`b71c43ddd18b22f21ecbfa6d3290140cd32761b7`。
- main.mjs SHA256：`4c8a5af0d1f10f47ab62c96f9ab98aa83d36e75b317c64ed650ebb4f15aac179`，Windows 产物与服务器一致。
- Node：官方 SHA256 校验的 22.23.2；systemd `daoyin-harness-cloud`，专用非 root 用户，回环 4700。
- backend 镜像：`60c141f90d955a5ea4decb43b9d243ee8ff1d821`，已写入服务器 BACKEND_IMAGE，避免后续 Compose 使用旧标签。
- 官网源码：`2a1b108265a21a526ecaf9582b5383ab7a06e1b4`，包含多轮按时间恢复与公开来源展示。
- [backend 定向发布](https://github.com/flyingrtx2333/daoyintech/actions/runs/33971146205) 成功。
- [官网首次定向发布](https://github.com/flyingrtx2333/daoyintech/actions/runs/33971616706) 和 [最终官网发布](https://github.com/flyingrtx2333/daoyintech/actions/runs/33972891106) 均成功。
- 最终官网 index-GAnC8Cxl.js SHA256 为 `c9a078c06d037eed7c59f741709aef133e8421c65b905faa457ed460ad480376`，HTTPS 下载与服务器文件一致；包含公开引用与恢复顺序修复。

Harness 无远端配置，代码本地提交后从 Git 对象构建并上传；主平台代码已推送。
共享工作区其他未提交修改没有纳入运行产物。发布初期发现并发提交后取消了三个误触发的工作流，随后仅定向发布 backend 与 frontend。

## 运行验证

- backend `/health` 返回 ok；Harness 健康为 available；无凭据云 API 返回 401。
- 生产迁移 agent_public_grants、agent_public_operations、agent_public_quotas 三表已存在。
- `AGENT_PUBLIC_ENABLED=1`、`AGENT_PUBLIC_WEBSITE_ENABLED=1`；官网配置返回 `engine: harness`。
- 真实任务 `run_d4eaa9be-2ec4-4440-be9d-743d2de209d6` 完成公开减压馆问答，产生 turn.started、tool.started、tool.completed、assistant.delta、turn.completed。
- 操作账本恰为两次 model、一次 search，均 completed；重复提交同 requestId 返回相同 Run。
- 模型、embedding、reranking、安全检查的真实用量均绑定该任务、操作和显式配置的官方付款成员。没有把访客提升为付款成员身份。
- 独立任务取消后状态为 cancelled；事件回放可查询，未认证读取被拒绝。
- 线上浏览器提交公开资料问答后显示检索成功与回答正文；刷新页面重新打开顾问后，原问题、检索记录和完整回答恢复。
- 最终版浏览器三轮回放顺序正确，三轮分别恢复 5、5、3 条公开资料引用；页面没有错误提示。
- Windows 验证：Harness typecheck / lint / 206 测试 / build；backend 81 项相关测试及新增开关测试 1 项；官网客户端 6 项测试、指定文件 ESLint 与构建。

可重复真实验收脚本：Windows 设置 DAOYIN_PRODUCTION_SMOKE=1 后运行
`node scripts/smoke-public-production.mjs`。脚本会调用真实模型，凭据仅存在内存，结果写入忽略目录 .cache。

## 回滚与限制

服务器保留 `/etc/daoyin-harness/backend.env.before-agent`、原镜像标签及
`/var/backups/daoyin-agent/frontend-before-20260905`。回滚先关官网开关，再恢复版本，保留数据。
服务配置与凭据不进入 Git。上线范围仅官网只读查询，尚未交付多 Worker、自动故障接管、统一钱包结算、跨业务记忆及业务写入。

## 独立工作台（2026-09-05 至 09-06）

用户明确要求部署独立 Harness 工作台。正式入口：<https://www.daoyintech.com/harness/>。
使用独立静态目录与 Nginx include，同源接入现有 BFF；本轮未更换 backend 镜像或 Harness 服务端。

- 工作台初始提交 `ea48ee1`；最终运行版本 `b396c047cc3d8cdca133787e2eac77c44d859758`。
- 初次浏览器验收发现原生 fetch 接收者绑定错误，已修复并增加回归测试；初始版本不应作为可用回滚目标。
- Windows typecheck、lint、216 项测试及完整 build 均通过。云端静态产物另从已提交 Git 对象构建，使用到的依赖版本与锁文件一致。
- HTTPS index.html SHA256：`ee931d8527dffe7ba1e510e85b94d7ea4865863aefbc8ede134ebc294482485c`。
- HTTPS workbench-YER3LWC7.js SHA256：`febdc0210402e776895a01ee61aeeac6aa9bb1b8fad0640d3bb0d748b393532f`。
- HTTPS workbench-O62ZTCN5.css SHA256：`f68e5561a51f85a9f86cd4d77aecc9a0352b428509128193193444065d8e595e`。
- 三个 HTTPS 文件与 Windows 发布清单、服务器文件一致；HTML 为 no-store，带 CSP、DENY 和 nosniff 响应头。
- Nginx 配置检查和平滑重载成功，后台健康仍为 ok，Harness 服务为 active / available。
- 线上浏览器真实任务 `run_5947b2da-1c96-4128-ab70-3eb3e3ddfd2b` 完成减压馆公开知识问答：372 字回答、5 个事件、5 条公开资料引用；页面与持久化终态一致。
- 1440×1000 桌面页面已检查并截图。刷新后在 390 像素宽度恢复原问题与回答，只有一轮对话，无错误提示和页面横向溢出，输入区域保持可见。
- 线上工作台点击停止生成后，`run_c3d76555-62c4-403d-9a97-1715ab51c2d7` 持久化为 cancelled，cancel_requested=1，保留 5 个事件；手机页面显示“任务已停止”，已完成的检索及 5 条引用仍可见。
- 390×844 手机菜单可展开并新建会话，随后从列表切回历史会话恢复原记录。768×1024 平板页面无横向溢出、无错误，键盘 Enter 可展开 5 条资料引用，焦点保持在 summary；临时浏览器尺寸已恢复。

当前应用为官网公开知识，访客授权 30 分钟；个人/企业账号永久历史、本机文件工具和业务写操作未接入此入口。
服务器工作台为 `/opt/daoyin-harness/workbench/current` 指向上述版本；后续版本可原子切换回该已验证版本。
截图保存在本机忽略目录 `.cache/workbench-acceptance/`，操作与维护说明见 `docs/CLOUD-WORKBENCH.md`。
