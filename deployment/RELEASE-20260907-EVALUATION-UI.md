# 2026-09-07 评估页实时计数与精简布局发布

## 发布版本

运行产物：`dd5f9bcd525cb2520df99cb2e29c0f7f5bf5d1ae`，已提交并推送至 main。工作台版本 `0.1.0`，构建时间 `2026-09-07T15:47:05.968Z`（UTC+8 23:47:05）。聊天运行时、静态工作台和独立评估服务均切换至这一提交；主平台后端无需修改。

普通发布脚本完成 readiness、公网 release.json、HTML 和所有哈希资源核验。独立评估切换确认无活动实验，健康返回 200，匿名 runner 返回 401。旧配置与链接备份在 `/var/backups/daoyin-agent/evaluation-20260907T154757Z`；普通发布日志在 `/opt/daoyin-harness/deployments/2026-09-07T15-47-46.504Z-dd5f9bc.json`。没有数据库迁移或清除既有会话、实验和业务记录。

## 改动

输入框是题目列表的唯一来源，一行即时计一题；空行不计，修改与删除同步更新。移除必须手点的「整理用例」，超出每批五次执行时明确提示，不静默截断。确认弹窗冻结本批输入、参数及费用授权后才提交，同编号恢复逻辑保留。

桌面采用输入主区与右侧历史实验，窄屏上下排列。运行设置及可选核对条件默认折叠；结果优先显示实际回复和关键观测，详细证据与元数据收进折叠区。管理员例行复查不再周期性隐藏表单。预算同步修正为题数 × 重复次数 × 单题调用上限，不再预留未启用的独立判分调用。

## 验证

UI TypeScript、相关文件 ESLint、Git 差异检查和正式产物打包通过，未运行历史模拟测试套件。

独立 Linux Chromium 使用实际组件和共享样式，通过本机运维转发连接线上主平台、Nginx、独立评估和真实账号运行时，没有伪造 API 或模型回复。确认即时计数、空行过滤、超限可见、重复次数同步、确认框内容一致、取消不创建实验、历史结果和无页面运行错误；1440×900、390px、320px 视口检查完成，窄屏未发现横向溢出。该方式未覆盖用户浏览器的 Cookie 登录流程，不能称为生产浏览器登录端到端验收。

新版提交入口执行一条真实样本，实验 **界面验收 · 实时题数**，编号 `ev_da902557b262477390b53b5288813869`。输入要求不使用工具或记忆，仅回复「实时题数已修复」。预算两次模型调用，实际一次，配置模型 `doubao-seed-2-1-pro-260628`；耗时 9.732 秒，任务 completed，1/1 完成，错误代码为空，实际回答包含要求文字。评估及云端版本均为上述发布提交，报告已持久化。

业务判定仍为 review，未把任务完成当成业务正确率验证；此次样本仅确认新提交与预算链路。UI 截图、临时构建文件、浏览器依赖和字体不纳入 Git；生产前端继续使用原字体栈。
