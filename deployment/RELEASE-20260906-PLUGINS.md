# 工作台插件入口发布

2026-09-06，按用户要求增加左侧插件入口与输入框左下角“＋”选择面板。
正式地址：<https://www.daoyintech.com/harness/>。

## 运行版本

- 源码及静态产物：`16f37ef2c37ca5e8658e4742772bc9d6d35bcd96`。
- index.html SHA256：`18aff53d6888de0a9788d05e454b40854073053def5e6849aa6991e62dd76e2a`。
- workbench-PQ2XT7J4.js SHA256：`f7f80edee37d96b15589fdc68d0f17f763b4b272def4d1faac12e54c48433d2a`。
- workbench-72JNN3XA.css SHA256：`021772d4805a8aee172b0e44173557e027ef1091685ef1eda5446ed95a24900a`。
- Windows 产物、上传文件、HTTPS 文件哈希一致；线上 release.json 指向上述版本。
- 静态 current 已原子切换，保留前一可用版本 `b396c047cc3d8cdca133787e2eac77c44d859758` 和旧哈希资源供回滚。
- 本轮仅发布独立工作台静态资源，未更换云端服务、主平台镜像或迁移业务数据。

## 范围与验证

官网知识可选；短剧制作、网站与应用、文旅影像列出已存在的业务能力并标明待接入。目录不签发权限，不开放未接入插件的执行；新会话开始模型调用前核对服务端 Profile。详细盘点见 [业务插件](../docs/PLUGINS.md)。

Windows 全量 typecheck、lint、219 项测试和 build 通过；最终焦点及文案调整后 UI 类型检查、指定目录 lint、13 项相关测试通过，发布从最终 Git 对象另行构建。

本地浏览器使用明确的授权/空会话 HTTP 夹具验收了插件目录、短剧搜索、选用后保留草稿、Esc 关闭并恢复焦点，以及手机加号面板。390 像素下页面与面板无横向溢出，面板处于视口内；1440 像素下目录为两列。夹具未调用模型，临时服务已停止。

正式页面已读取真实访客授权及既有会话，侧栏插件入口、加号、官网知识标记与原回放记录正常。此次插件交互验收不新建付费模型或短剧生成任务。

线上加号面板显示官网知识已选、三项业务待接入；“浏览全部插件”可搜索到短剧的真实能力说明。验证期间原 30 分钟访客授权到期，页面按既有策略清空受保护会话并要求重新进入，输入草稿仍保留；未把这次到期后的空历史误记为插件切换回放成功。
