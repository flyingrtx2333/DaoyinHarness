# Incremental workbench replies

Sending previously incremented a refresh revision that cleared the entire transcript and restarted replay from zero. The frontend now retains message nodes and the event cursor, shows the new question immediately, appends reply chunks and displays actual tool operations with a spinner and elapsed time. Interrupted reads recover without erasing history. Account/session changes retain the existing isolation rules.

The platform's public and account model bridges now negotiate actual provider streaming. Harness persists each public text chunk before browser replay and waits for the validated, accounted final result. Raw reasoning/tool arguments stay off the display stream. See [ADR-0013](../docs/adr/0013-incremental-cloud-replies.md). Browser updates use incremental 500ms polling, so chunk visibility also depends on network and backend response time.

Windows verification: typecheck/lint/build, 306 unit tests, streaming browser checks at 1280/390/320px and account/menu/plugin regression at 1280/1920/390/320px. Browser fixtures cover retained DOM, immediate send state, tool spinner, partial text, transient failure recovery, cancellation, retained text and continuing replay cursors. Main-platform isolated Docker/MySQL tests: 46 passed.

Released 2026-09-06: Harness runtime and static revision `d785c1a085f979e8ad6a933c3769728732bc763d`; platform backend `9bc2adda1eec3b4e7143a71fb8a0ecd6cd74933f`. [Platform deployment workflow](https://github.com/flyingrtx2333/daoyintech/actions/runs/34019347565) succeeded. The backend image was pinned in its Compose environment after verifying the running container version. All four production static asset hashes match the committed release. Runtime health, active lease and backend health passed; event/session/run/memory row counts were unchanged across the runtime cutover. The pre-cutover database backup and previous runtime/static targets are recorded under `/var/backups/daoyin-agent/streaming-20260906/`. No schema migration was introduced.

At 07:35 UTC a real public-visitor browser called the production model and public knowledge tool. One tool start/completion and 64 text-delta events were observed; partial text was visible while the run was still active. First public text arrived at 12.797s (including the knowledge query); the run completed at 19.229s, with no page errors. This verifies real incremental output, not a typewriter animation. Signed-in account/menu checks used mocked account APIs against the deployed assets; this public run does not claim acceptance using the user's private account. The foundation runtime still reports `productionReady: false`; this release does not claim distributed scheduler readiness.
